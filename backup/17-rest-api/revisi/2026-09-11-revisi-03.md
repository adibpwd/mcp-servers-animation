# Revisi 03 — Visual-First: Ganti Teks → SVG Icon Inline, Minim Teks (17-rest-api)

> Status: **📝 PLAN ONLY, belum dieksekusi.**
> Arah: bukan sekadar kurangi teks via popOut/timing (itu sudah
> direncanakan), tapi ganti KONSEP teks menjadi VISUAL ICON — user mau
> minim teks, banyak icon. Semua icon SVG inline (tidak ada PNG baru,
> tidak perlu generate ChatGPT).

---

## 1. Prinsip

- **Teks di layar: hanya caption bar (1 kalimat) + label pendek di dalam
  icon/badge.** Semua blok teks bebas (note, payoff card teks panjang,
  speech bubble paragraf) diganti ikon + label max 2–3 kata.
- **Setiap konsep = 1 visual.** Misal: PUT vs PATCH bukan 2 baris teks,
  tapi 2 kartu dengan isi item yang berbeda warna secara visual.
- **SVG inline** — semua ikon baru dibuat sebagai komponen SVG di
  `Animation.jsx`. Tidak ada PNG baru. Tidak ada generate ChatGPT.
- **PopOut wajib** tiap beat sebelum elemen berikutnya masuk.
  Aturan: max 2–3 elemen di layar bersamaan (anchor persisten tidak
  dihitung).

---

## 2. Audit Elemen Teks Aktif per Act — Sebelum vs Sesudah

### Act 1 (7s)

| Elemen sekarang | Tipe | Ganti jadi | Alasan |
|---|---|---|---|
| `continuityTag` rect + teks italic | teks | **SVG ChainLink inline** — ikon rantai/link kecil + label "dari ep. sebelumnya" max 3 kata | lebih compact, tidak perlu baca kalimat |
| `signageBadge` StarburstBadge + teks "REST API" | teks besar | **Tetap** — ini branding, teks di sini valid | judul topik, wajar ada teks |
| `act1QuestionBubble` SpeechBubble + teks panjang | teks | **Ganti `QuestionMark` visual** — tanda tanya besar beranimasi di atas gedung, tanpa speech bubble | visual lebih langsung, tidak perlu dibaca |
| `act1PayoffCard` StarburstBadge + teks | teks | **Ganti `ConsistencyBadge`** — ikon = lingkaran + 3 panah melingkar (simbol konsistensi/siklus), label "Konsisten" 1 kata | icon-first |
| 3× `moveEnvelope` terbang | visual | Potong jadi 1× | terlalu ramai |

---

### Act 2 (8s)

| Elemen sekarang | Tipe | Ganti jadi | Alasan |
|---|---|---|---|
| `DoorBox` ×3 — label `/getUser`, `/user_remove`, `/newCustomer` | teks di dalam kotak | **Tetap labelnya** — ini adalah poin utama Act 2 (endpoint jelek vs bagus), teks di sini = konten, bukan narasi | tidak bisa diganti icon tanpa hilang maknanya |
| `xMark-0`, `xMark-1` — garis silang | visual | **Tetap** — sudah visual | ok |
| `insightBadge` StarburstBadge + teks "Tiap restoran, aturan beda" | teks | **Ganti `ChaosBadge`** — ikon = tanda seru (!!) besar + tanda tanya (??) di sebelahnya, label "kacau" 1 kata | emotion > kalimat |
| `act2PayoffCard` SpeechBubble + teks | teks | **Ganti `RuleIcon`** — ikon = papan peraturan (persegi dengan garis-garis horizontal = aturan), label "→ aturan sama" | arah, bukan penjelasan |
| `orderAnchor` `OrderFormCard` mode freeform | visual | Digeser ke setelah kekacauan (bukan di awal) | narratively lebih pas |

---

### Act 3 (10s)

| Elemen sekarang | Tipe | Ganti jadi | Alasan |
|---|---|---|---|
| `oldStrike` rect + teks endpoint lama + garis coret | teks | **Tetap tapi ringkas** — endpoint lama (`/getUser`) + garis coret sudah visual yang efektif. Hapus sub-teks di bawahnya. | teks URL = konten, garis coret = visual, ok |
| `newReveal` rect + teks endpoint baru + sub-teks pertanyaan | teks | **Tetap endpoint baru** (`/users/123`), hapus sub-teks "Aksinya taruh mana?" — itu fungsinya dipindah ke caption bar | 1 teks, bukan 2 |
| 5× `methodBadge` — rect + icon PNG + teks method + label bawah | icon + teks | **Perbesar icon PNG** (dari 22px ke 36px), **hapus label bawah** (teks "Lihat", "Buat Baru", dst) — icon harus cukup tanpa label | label bawah = teks redundan kalau icon jelas |
| `putPatchNote` rect 2 baris teks | teks panjang | **Ganti `PutPatchComparison` SVG** — 2 kolom, isi item visual (checkmark vs X), TANPA teks paragraf | visual beats text |
| `act3PayoffCard` SpeechBubble + teks | teks | **Hapus** — caption bar sudah cukup | duplikat |
| `DoorBox` morph ke resource-based | label teks | **Tetap** — resource path = konten utama Act 3 | ok |

**`PutPatchComparison` detail visual:**
```
┌──── PUT ────┐     ┌──── PATCH ────┐
│ ✓ nasi goreng│     │ ✓ nasi goreng │
│ ✓ telur     │     │ ✓ telur       │
│ ✓ jus       │     │ ✓→ jus (ubah) │   ← item berubah: warna WARNING
│ (ganti semua)│     │ (sebagian)    │
└─────────────┘     └───────────────┘
```
Checkmark = ikon SVG inline (bukan karakter teks "✓"). Warna item
normal = `COLORS.MUTED`, item berubah = `COLORS.WARNING`. Header PUT =
border `COLORS.TECHNICAL`, header PATCH = border `COLORS.WARNING`.
Label di dalam kolom max 2 kata, bukan kalimat.

---

### Act 4 (11s)

| Elemen sekarang | Tipe | Ganti jadi | Alasan |
|---|---|---|---|
| `freeformNote` rect + teks "Susah dibaca dapur" | teks | **Ganti `ConfusedIcon`** — ikon kepala dapur dengan tanda tanya di atas, label "?" saja | 1 simbol > 1 kalimat |
| `jsonFormCard` rect + 3 field label:value | teks terstruktur | **Tetap** — ini content utama (JSON structure), teks = konten | ok |
| `jsonNote` rect + teks "Sekarang dapur baca format sama" | teks | **Hapus** — ganti dengan `CheckChef` inline: ikon tangan jempol/check kecil di pojok `jsonFormCard` | visual validasi, bukan kalimat |
| `jsonFormatNote` teks italic kecil | teks catatan | **Hapus** — terlalu kecil dan teknis untuk dibaca di animasi | tidak ada ruang di 44 detik |
| `requestResourceLabel` rect + teks path | teks | **Hapus** — sudah ada di dalam masing-masing `RequestCard` | duplikat |
| `request1Card` + `request2Card` | visual card teks | **Tetap tapi compact** — hapus label header ("REQUEST 1"/"REQUEST 2"), resource path jadi lebih besar, field key:value tetap | kurangi baris teks di dalam card |
| `statelessNote` rect 2 baris | teks panjang | **Ganti `StatelessIcon`** — ikon 2 kartu dengan panah pemisah + label "berdiri sendiri" | visual konsep |
| `act4PayoffCard` StarburstBadge + 2 baris teks | teks besar | **Ganti `PayoffCheckmark`** — ikon centang besar hijau + label 2 kata "Lebih predictable" | ikon > starburst teks |
| `act4Clarify` teks italic | teks kecil | **Hapus** — terlalu kecil dibaca | tidak visible di 44 detik |
| `act4Cliffhanger` teks italic | teks | **Ganti `CliffhangerIcon`** — ikon kunci + label "Aman?" 1 kata, warna WARNING | transisi ke Act 5 |

---

### Act 5 (8s)

| Elemen sekarang | Tipe | Ganti jadi | Alasan |
|---|---|---|---|
| `stamp200` + `stamp201` — lingkaran + icon PNG + teks code + label | icon + teks | **Tetap** — icon sudah dominan, 2 teks kecil (200/201) = konten penting | ok |
| resource rahasia terbang (`flyEnv` + teks "DATA RAHASIA") | teks overlay | **Ganti teks** "DATA RAHASIA" → **ikon gembok merah** terbang (SVG inline `LockFlying`), tanpa teks | visual beats label |
| `lockedBadge` icon PNG gembok | icon | **Tetap** — sudah icon | ok |
| `act5PayoffCard` SpeechBubble + teks | teks | **Ganti `ConsistencyCheck`** — ikon centang besar hijau, label "Konsisten ✓" | ringkas |
| `act5TeaseBubble` SpeechBubble + teks | teks | **Ganti `AuthTeaser`** — ikon kunci + label "Auth →" + panah besar | 2 kata, bukan kalimat |
| `act5NextTopic` StarburstBadge + 2 baris teks | teks | **Tetap** — ini CTA akhir, teks diperlukan untuk judul topik berikutnya | ok |

---

## 3. Komponen SVG Baru (Semua Inline, 0 PNG Baru)

### 3.1 `ChainLinkBadge` — Act 1, continuity

```jsx
const ChainLinkBadge = () => (
  <g>
    <rect x={-70} y={-18} width={140} height={36} rx={18}
      fill={COLORS.PANEL} stroke={COLORS.MUTED} strokeWidth={1.2} />
    {/* 2 cincin rantai */}
    <circle cx={-20} cy={0} r={9} fill="none" stroke={COLORS.MUTED} strokeWidth={2.5} />
    <circle cx={8} cy={0} r={9} fill="none" stroke={COLORS.MUTED} strokeWidth={2.5} />
    {/* overlap tengah (tutup gap) */}
    <rect x={-14} y={-6} width={18} height={12} fill={COLORS.PANEL} />
    <text x={28} y={5} fill={COLORS.MUTED} fontSize={10} fontStyle="italic">ep. lalu</text>
  </g>
)
```

### 3.2 `LargeQuestionMark` — Act 1, ganti speech bubble pertanyaan

```jsx
const LargeQuestionMark = ({ color = COLORS.WARNING }) => (
  <g>
    <text textAnchor="middle" y={0} fontSize={96} fontWeight={900}
      fill={color} opacity={0.85} filter="url(#glow)">?</text>
  </g>
)
```

Muncul di atas gedung (y = ~BUILDING_POS.y - 120), warna WARNING,
glow filter. PopOut setelah 1.2s, baru `ConsistencyBadge` popIn.

### 3.3 `ConsistencyBadge` — Act 1, ganti payoff card

```jsx
// 3 panah melingkar = simbol "konsisten/siklus" universal
const ConsistencyBadge = ({ color = COLORS.SUCCESS }) => {
  const R = 38
  return (
    <g>
      <circle r={56} fill={COLORS.PANEL} stroke={color} strokeWidth={2.5} />
      {/* 3 segmen arc panah melingkar */}
      {[0, 120, 240].map((deg, i) => {
        const rad = (deg * Math.PI) / 180
        const x1 = Math.cos(rad) * R
        const y1 = Math.sin(rad) * R
        const rad2 = ((deg + 90) * Math.PI) / 180
        const x2 = Math.cos(rad2) * R
        const y2 = Math.sin(rad2) * R
        return (
          <path key={i}
            d={`M ${x1} ${y1} A ${R} ${R} 0 0 1 ${x2} ${y2}`}
            fill="none" stroke={color} strokeWidth={4} strokeLinecap="round" />
        )
      })}
      <text y={90} textAnchor="middle" fill={color}
        fontSize={13} fontWeight={700}>Konsisten</text>
    </g>
  )
}
```

### 3.4 `ChaosBadge` — Act 2, ganti insightBadge

```jsx
const ChaosBadge = ({ color = COLORS.WARNING }) => (
  <g>
    <circle r={52} fill={COLORS.PANEL} stroke={color} strokeWidth={2.5} />
    <text x={-16} y={8} textAnchor="middle" fontSize={44} fontWeight={900}
      fill={color} opacity={0.9}>?</text>
    <text x={16} y={8} textAnchor="middle" fontSize={44} fontWeight={900}
      fill={COLORS.ERROR} opacity={0.9}>!</text>
    <text y={74} textAnchor="middle" fill={color}
      fontSize={12} fontWeight={700}>kacau</text>
  </g>
)
```

### 3.5 `RuleIcon` — Act 2, ganti act2PayoffCard

```jsx
// Papan aturan: persegi dengan 3 garis horizontal = "daftar aturan"
const RuleIcon = ({ color = COLORS.SUCCESS }) => (
  <g>
    <rect x={-52} y={-52} width={104} height={104} rx={14}
      fill={COLORS.PANEL} stroke={color} strokeWidth={2.2} />
    {/* 3 baris = aturan */}
    {[-20, 0, 20].map((y, i) => (
      <g key={i}>
        <circle cx={-30} cy={y} r={4} fill={color} />
        <rect x={-18} y={y - 3} width={52} height={6} rx={3}
          fill={color} opacity={i === 0 ? 0.9 : i === 1 ? 0.6 : 0.35} />
      </g>
    ))}
    <text y={70} textAnchor="middle" fill={color}
      fontSize={11} fontWeight={700}>→ aturan sama</text>
  </g>
)
```

### 3.6 `PutPatchComparison` — Act 3

```jsx
const PutPatchComparison = () => {
  const COL_W = 230
  const COL_H = 190
  const HEADER_H = 38
  const ROW_H = 32

  const putItems  = ['nasi goreng', 'telur', 'jus']
  const patchItems = [
    { label: 'nasi goreng', changed: false },
    { label: 'telur', changed: false },
    { label: 'jus', changed: true },
  ]

  const Checkmark = ({ x, y, color }) => (
    <path d={`M ${x - 5} ${y} L ${x - 1} ${y + 4} L ${x + 6} ${y - 5}`}
      stroke={color} strokeWidth={2.5} fill="none" strokeLinecap="round" />
  )

  const renderPut = (x) => (
    <g transform={`translate(${x}, 0)`}>
      <rect x={-COL_W / 2} y={0} width={COL_W} height={COL_H} rx={12}
        fill={COLORS.PANEL} stroke={COLORS.TECHNICAL} strokeWidth={2} />
      <text x={0} y={HEADER_H / 2 + 6} textAnchor="middle"
        fill={COLORS.TECHNICAL} fontSize={15} fontFamily="monospace" fontWeight={800}>PUT</text>
      <line x1={-COL_W/2+12} y1={HEADER_H} x2={COL_W/2-12} y2={HEADER_H}
        stroke={COLORS.TECHNICAL} strokeWidth={1} opacity={0.4} />
      {putItems.map((item, i) => (
        <g key={i} transform={`translate(0, ${HEADER_H + 10 + i * ROW_H})`}>
          <Checkmark x={-COL_W/2+22} y={6} color={COLORS.TECHNICAL} />
          <text x={-COL_W/2+38} textAnchor="start"
            fill={COLORS.TEXT} fontSize={13} fontFamily="monospace">{item}</text>
        </g>
      ))}
      <text x={0} y={COL_H - 10} textAnchor="middle"
        fill={COLORS.TECHNICAL} fontSize={10} opacity={0.6} fontStyle="italic">ganti semua</text>
    </g>
  )

  const renderPatch = (x) => (
    <g transform={`translate(${x}, 0)`}>
      <rect x={-COL_W / 2} y={0} width={COL_W} height={COL_H} rx={12}
        fill={COLORS.PANEL} stroke={COLORS.WARNING} strokeWidth={2} />
      <text x={0} y={HEADER_H / 2 + 6} textAnchor="middle"
        fill={COLORS.WARNING} fontSize={15} fontFamily="monospace" fontWeight={800}>PATCH</text>
      <line x1={-COL_W/2+12} y1={HEADER_H} x2={COL_W/2-12} y2={HEADER_H}
        stroke={COLORS.WARNING} strokeWidth={1} opacity={0.4} />
      {patchItems.map((item, i) => (
        <g key={i} transform={`translate(0, ${HEADER_H + 10 + i * ROW_H})`}>
          {item.changed
            ? <path d={`M ${-COL_W/2+16} 2 L ${-COL_W/2+28} 10 M ${-COL_W/2+28} 2 L ${-COL_W/2+16} 10`}
                stroke={COLORS.WARNING} strokeWidth={2.5} strokeLinecap="round" />
            : <Checkmark x={-COL_W/2+22} y={6} color={COLORS.MUTED} />
          }
          <text x={-COL_W/2+38} textAnchor="start"
            fill={item.changed ? COLORS.WARNING : COLORS.MUTED}
            fontSize={13} fontFamily="monospace"
            fontWeight={item.changed ? 700 : 400}>{item.label}</text>
          {item.changed && (
            <text x={COL_W/2-8} textAnchor="end"
              fill={COLORS.WARNING} fontSize={10}>← ubah</text>
          )}
        </g>
      ))}
      <text x={0} y={COL_H - 10} textAnchor="middle"
        fill={COLORS.WARNING} fontSize={10} opacity={0.6} fontStyle="italic">ubah sebagian</text>
    </g>
  )

  return (
    <g>
      {renderPut(-140)}
      {renderPatch(140)}
    </g>
  )
}
```

### 3.7 `ConfusedChefIcon` — Act 4, ganti freeformNote

```jsx
// Kepala koki + tanda tanya = "dapur bingung"
const ConfusedChefIcon = ({ color = COLORS.WARNING }) => (
  <g>
    <circle r={44} fill={COLORS.PANEL} stroke={color} strokeWidth={2} />
    {/* topi koki */}
    <rect x={-20} y={-50} width={40} height={22} rx={6}
      fill={COLORS.PANEL} stroke={color} strokeWidth={2} />
    <rect x={-28} y={-32} width={56} height={8} rx={4}
      fill={color} opacity={0.4} />
    {/* wajah bingung: alis turun */}
    <path d="M -16 -16 Q -8 -22 0 -16" stroke={color} strokeWidth={2.5}
      fill="none" strokeLinecap="round" />
    <path d="M 0 -16 Q 8 -22 16 -16" stroke={color} strokeWidth={2.5}
      fill="none" strokeLinecap="round" />
    {/* mulut datar */}
    <path d="M -10 2 Q 0 -2 10 2" stroke={color} strokeWidth={2}
      fill="none" strokeLinecap="round" />
    {/* tanda tanya di pojok kanan atas */}
    <text x={36} y={-32} fontSize={22} fontWeight={900}
      fill={COLORS.WARNING}>?</text>
  </g>
)
```

### 3.8 `StatelessDivider` — Act 4, antara 2 request card

```jsx
// Dashed vertical line + label "baru" — connector antara 2 request card
const StatelessDivider = () => (
  <g>
    <line x1={0} y1={-50} x2={0} y2={50}
      stroke={COLORS.BORDER} strokeWidth={1.5} strokeDasharray="5,5" />
    <rect x={-22} y={-12} width={44} height={24} rx={12}
      fill={COLORS.PANEL} stroke={COLORS.BORDER} strokeWidth={1} />
    <text x={0} y={5} textAnchor="middle"
      fill={COLORS.MUTED} fontSize={10} fontStyle="italic">baru</text>
  </g>
)
```

### 3.9 `LockFlying` — Act 5, ganti "DATA RAHASIA" teks terbang

```jsx
// Gembok merah terbang — gantikan teks "DATA RAHASIA"
const LockFlying = ({ color = COLORS.ERROR }) => (
  <g>
    {/* badan gembok */}
    <rect x={-16} y={-4} width={32} height={26} rx={6}
      fill={COLORS.PANEL} stroke={color} strokeWidth={2.5} />
    {/* lubang kunci */}
    <circle cx={0} cy={8} r={5} fill="none" stroke={color} strokeWidth={2} />
    <rect x={-2} y={10} width={4} height={7} rx={1} fill={color} />
    {/* lengkung atas */}
    <path d="M -12 -4 A 12 14 0 0 1 12 -4" fill="none"
      stroke={color} strokeWidth={3} strokeLinecap="round" />
  </g>
)
```

Dipakai sebagai `flyLock` (state terpisah dari `flyEnv`) yang terbang dari
posisi `SECRET_ENVELOPE_START` ke `SECRET_ENVELOPE_END`. `flyEnv` lama
tidak lagi dipakai di Act 5 (masih dipakai di Act 1 untuk 1× envelope
terbang).

### 3.10 `AuthTeaser` — Act 5, ganti act5TeaseBubble

```jsx
// Ikon kunci + label + panah → tease topik Auth
const AuthTeaser = ({ color = COLORS.WARNING }) => (
  <g>
    <rect x={-120} y={-36} width={240} height={72} rx={18}
      fill={COLORS.PANEL} stroke={color} strokeWidth={2} />
    {/* gembok kecil */}
    <rect x={-84} y={-14} width={22} height={18} rx={4}
      fill="none" stroke={color} strokeWidth={2} />
    <path d="M -78 -14 A 7 8 0 0 1 -62 -14" fill="none"
      stroke={color} strokeWidth={2.5} strokeLinecap="round" />
    <circle cx={-73} cy={-2} r={3} fill="none" stroke={color} strokeWidth={1.5} />
    {/* teks */}
    <text x={-28} y={-10} textAnchor="start"
      fill={color} fontSize={14} fontWeight={700}>Siapa</text>
    <text x={-28} y={12} textAnchor="start"
      fill={color} fontSize={14} fontWeight={700}>yang boleh?</text>
    {/* panah ke kanan */}
    <text x={78} y={6} textAnchor="middle"
      fill={color} fontSize={20} fontWeight={900}>→</text>
  </g>
)
```

---

## 4. Perubahan `data.js`

| Constant | Aksi | Alasan |
|---|---|---|
| `ACT1_QUESTION` | **Hapus** | Diganti visual `LargeQuestionMark` |
| `ACT1_PAYOFF` | **Ganti teks** → `'Konsisten'` (1 kata, label di `ConsistencyBadge`) | cukup 1 kata |
| `ACT2_INSIGHT` | **Hapus** | Diganti `ChaosBadge` — icon tidak butuh string panjang |
| `ACT2_PAYOFF` | **Ganti teks** → `'aturan sama'` (label di `RuleIcon`) | label pendek |
| `ACT3_PUT_PATCH_NOTE` | **Hapus** | Diganti `PutPatchComparison` SVG |
| `ACT3_IDEMPOTENCY_NOTE` | **Hapus** | Terlalu teknis, di luar scope visual |
| `ACT3_PAYOFF` | **Hapus** | `act3PayoffCard` dihapus |
| `ACT4_FREEFORM_NOTE` | **Hapus** | Diganti `ConfusedChefIcon` |
| `ACT4_JSON_NOTE` | **Hapus** | Diganti centang kecil di sudut `jsonFormCard` |
| `ACT4_JSON_FORMAT_NOTE` | **Hapus** | Terlalu kecil, dihapus total |
| `ACT4_REQUEST_RESOURCE` | **Hapus** | Label terpisah dihapus |
| `ACT4_STATELESS_NOTE` | **Ganti** → `'berdiri sendiri'` (label `StatelessDivider`) | 2 kata |
| `ACT4_STATELESS_CAPTION` | **Hapus** | Duplikat |
| `ACT4_PAYOFF` | **Ganti** → `'Lebih predictable'` (label `PayoffCheckmark`) | 2 kata |
| `ACT4_STATELESS_CLARIFY` | **Hapus** | Terlalu kecil, tidak terbaca |
| `ACT4_CLIFFHANGER` | **Ganti** → `'Aman?'` (label `CliffhangerIcon`) | 1 kata |
| `ACT5_SECRET_LABEL` | **Hapus** | Diganti visual `LockFlying` (gembok terbang, tanpa teks) |
| `ACT5_PAYOFF` | **Ganti** → `'Konsisten ✓'` | 2 kata |
| `ACT5_TEASE` | **Hapus** | Diganti `AuthTeaser` SVG (teks sudah di dalam SVG) |
| `ACT3_PUT_ITEMS` + `ACT3_PATCH_ITEMS` | **Tambah** | Data item untuk `PutPatchComparison` |

**Constant yang TETAP (tidak berubah):**
`ACT1_CONTINUITY_LINE` (label ChainLink), `ACT1_SIGNAGE`, `ACT2_DOORS`,
`ACT3_OLD_ENDPOINT`, `ACT3_NEW_ENDPOINT`, `ACT3_METHODS`,
`ACT4_REQUEST1_LABEL`, `ACT4_REQUEST1_FIELDS`, `ACT4_REQUEST2_LABEL`,
`ACT4_REQUEST2_FIELDS`, `ACT4_JSON_FIELDS`,
`ACT5_STATUS_200`, `ACT5_STATUS_201`, `ACT5_REST_NOTE`, `ACT5_NEXT_TOPIC`,
semua `PHASES`, `COLORS`, `SFX_MAP`, `TOTAL_DURATION`.

---

## 5. Perubahan Timeline & PopOut

### Aturan global yang diterapkan ke semua Act:

```
popIn(elemen A)
→ tahan [durasi baca]
→ popOut(elemen A)      ← BARU, wajib sebelum elemen berikutnya
→ popIn(elemen B)
```

### Act 1

```
t+0.0  popIn buildingAnchor, customerAnchor
t+0.3  popIn continuityTag (ChainLinkBadge)
t+1.3  popOut continuityTag
t+1.5  popIn signageBadge
t+2.5  moveEnvelope ×1 (bukan 3×)
t+3.2  popIn questionMark (LargeQuestionMark di atas gedung)
t+4.4  popOut questionMark
t+4.6  popIn consistencyBadge
```

### Act 2

```
t+0.0  reset doors (label lama jelek, good=false)
t+0.2  popIn door-0, door-1, door-2 berurutan
t+1.1  popIn orderAnchor  ← DIPINDAH: dulu t+1.1 sebelum kekacauan, sekarang...
         → ubah ke t+5.0 (sesudah xMark-1 selesai)
t+1.7  moveEnvelope → door-0
t+2.3  popIn xMark-0
t+2.7  moveEnvelope balik
t+3.4  moveEnvelope → door-1
t+4.0  popIn xMark-1
t+4.4  moveEnvelope balik
t+5.0  popIn orderAnchor  ← posisi baru
t+5.4  popIn chaosBadge
t+6.6  popOut chaosBadge
t+6.8  popIn ruleIcon
```

### Act 3

```
t+0.2  popIn oldStrike
t+1.6  popOut oldStrike
t+1.8  popIn newReveal (tanpa sub-teks)
t+2.5  popOut newReveal (atau fade opacity ke 0.3 biar jadi bg)
t+2.6  popIn methodBadge-0 (GET)
t+3.0  popIn methodBadge-1 (POST)
t+3.4  popIn methodBadge-2 (PUT)
t+3.8  popIn methodBadge-3 (PATCH)
t+4.2  popIn methodBadge-4 (DELETE)
         → 5 badge tetap terlihat (compact, icon-only tanpa label bawah)
t+4.8  morph doorLabels → resource-based
t+5.4  popIn putPatchComparison (SVG 2 kolom)
         → tahan sampai akhir Act 3
```

### Act 4

```
t+0.2  popIn confusedChefIcon (ganti freeformNote)
         orderAnchor mode=freeform terlihat
t+1.4  popOut confusedChefIcon
t+1.6  switch orderMode → structured
t+1.8  popIn jsonFormCard
         (checkChef kecil muncul di sudut jsonFormCard setelah 0.8s)
t+3.0  [Beat B]
         popOut jsonFormCard
t+3.2  popIn request1Card
t+4.2  popIn statelessDivider (antara 2 card)
t+4.4  popIn request2Card
t+5.6  popIn statelessNote (1 baris teks pendek)
t+6.6  popOut request1Card, request2Card, statelessNote, statelessDivider
t+6.8  popIn act4PayoffLine (teks "Lebih predictable", tanpa starburst)
t+7.6  popOut act4PayoffLine
t+7.8  popIn cliffhangerLock (ikon kunci + "Aman?")
```

### Act 5

```
t+0.2  popIn stamp200
t+0.8  popIn stamp201
t+1.8  flyLock: LockFlying terbang SECRET_START → SECRET_END
t+2.6  popIn lockedBadge
t+3.2  popOut stamp200, stamp201
t+3.6  popIn consistencyCheck (centang hijau + "Konsisten ✓")
t+4.4  popOut consistencyCheck
t+4.6  popIn authTeaser (AuthTeaser SVG)
t+5.8  popOut authTeaser
t+6.0  popIn act5NextTopic (StarburstBadge → topik berikutnya)
```

---

## 6. State Baru / Dihapus

**Ditambah:**
- `flyLock` — `{ x, y, opacity }` untuk LockFlying Act 5
- `pop['questionMark']`
- `pop['consistencyBadge']`
- `pop['chaosBadge']`
- `pop['ruleIcon']`
- `pop['confusedChef']`
- `pop['statelessDivider']`
- `pop['act4PayoffLine']`
- `pop['cliffhangerLock']`
- `pop['consistencyCheck']`
- `pop['authTeaser']`
- `pop['putPatchComparison']`

**Dihapus dari pop:**
- `pop['continuityTag']` → diganti `ChainLinkBadge` tapi TETAP pakai pop
  state (tidak berubah mekanismenya, cuma komponen JSX-nya ganti)
- `pop['act1QuestionBubble']` → ganti `pop['questionMark']`
- `pop['act1PayoffCard']` → ganti `pop['consistencyBadge']`
- `pop['insightBadge']` → ganti `pop['chaosBadge']`
- `pop['act2PayoffCard']` → ganti `pop['ruleIcon']`
- `pop['putPatchNote']` → ganti `pop['putPatchComparison']`
- `pop['act3PayoffCard']` → hapus
- `pop['freeformNote']` → ganti `pop['confusedChef']`
- `pop['jsonNote']` → hapus
- `pop['jsonFormatNote']` → hapus
- `pop['requestResourceLabel']` → hapus
- `pop['statelessNote']` → ganti `pop['statelessDivider']` + 1 baris teks
- `pop['act4PayoffCard']` → ganti `pop['act4PayoffLine']`
- `pop['act4Clarify']` → hapus
- `pop['act4Cliffhanger']` → ganti `pop['cliffhangerLock']`
- `pop['act5PayoffCard']` → ganti `pop['consistencyCheck']`
- `pop['act5TeaseBubble']` → ganti `pop['authTeaser']`

**Net: hapus 15 pop state lama, tambah 12 pop state baru.**
Elemen di layar per beat turun dari rata-rata 5–7 ke 2–3.

---

## 7. Ringkasan Elemen per Act Setelah Revisi

| Act | Elemen teks aktif maks | Elemen visual (icon/shape) maks | Total maks |
|---|---|---|---|
| Intro | 2 (title + subtitle typing) | 0 | 2 |
| Act 1 | 1 (caption bar saja) | 3 (gedung, pelanggan, badge) | 3 |
| Act 2 | 1 (caption bar) + label di DoorBox | 4 (3 door + 1 badge) | 5 |
| Act 3 | 1 (caption bar) + endpoint di box | 6 (5 method badge + PutPatch) | ~7 |
| Act 4 | 1 (caption bar) + field di card | 4 (2 request card + divider + note) | ~5 |
| Act 5 | 1 (caption bar) + 2 kode stamp | 4 (stamp×2, lockedBadge, authTeaser) | ~6 |

---

## 8. File yang Berubah

| File | Perubahan |
|---|---|
| `Animation.jsx` | Tambah 10 komponen SVG baru, hapus 15 pop-state render block, ubah timeline semua Act, tambah `flyLock` state |
| `data.js` | Hapus 14 constant, ganti teks 6 constant, tambah 2 constant baru |
| File lain | Tidak ada |

---

## 9. Yang TIDAK Berubah

- Semua mekanisme GSAP, `seededRandom01`, `typeLine`, `popIn`/`popOut`,
  `moveEnvelope`, `sfxOn`, `say`
- Intro morph hijau-biru (revisi-01)
- `COLORS`, `PHASES` (durasi, badge label, badgeColor), `SFX_MAP`
- Semua icon PNG yang ada (`method-get/post/put/patch/delete`,
  `stamp-200/201`, `structured-form`, `post-building`, dll)
- `DoorBox` komponen (tetap dipakai Act 2 & 3)
- `RequestCard` komponen (tetap dipakai Act 4, cukup compact tanpa label header)
- `manifest.js`, `registry.js`, `icons/`, `revisi/README.md`

---

## 10. Checklist Eksekusi

- [ ] Tambah 10 komponen SVG baru di `Animation.jsx` (bagian atas,
      sebelum `export default`)
- [ ] Tambah `flyLock` state (mirip pola `flyEnv`)
- [ ] Tambah `ACT3_PUT_ITEMS` + `ACT3_PATCH_ITEMS` ke `data.js`
- [ ] Hapus 14 constant yang sudah tidak dipakai dari `data.js`
- [ ] Ganti teks 6 constant di `data.js`
- [ ] Rewrite render block semua Act di JSX (hapus 15 elemen lama,
      render 12 elemen baru)
- [ ] Rewrite timeline semua Act — popOut wajib sebelum popIn berikutnya
- [ ] Hapus import constant yang dihapus dari `data.js` di baris import
      `Animation.jsx`
- [ ] Preview browser — tiap beat max 3 elemen aktif, tidak ada teks
      blok panjang, semua icon terbaca tanpa label
- [ ] Update `revisi/README.md` — update entry revisi-03 status
