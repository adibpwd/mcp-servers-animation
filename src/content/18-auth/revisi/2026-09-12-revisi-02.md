# Revisi-02 — Protagonis Nina → Adib

- **Tanggal:** 2026-09-12
- **Topik:** 18-auth (Authentication)
- **Status test:** ✅ `esbuild` bundle Animation.jsx + data.js exit 0
  (setelah rename). vite dev transform `/src/content/18-auth/Animation.jsx`
  → 200. Preview visual masih menunggu reviewer.
- **Trigger:** Instruksi user — "nina ganti adib aja ya" — menyelaraskan
  protagonis 18-auth dengan protagonis seri aktif 17-rest-api (Adib), sesuai
  Series Identity Contract (09 §1.Q: kesamaan karakter seri).

## Perubahan (per 02-standar-konten.md §222-228)

- **State:** rename identitas berbasis nama variabel (tanpa mengubah jumlah/
  jenis state):
  - `data.js`: `NINA` → `ADIB` (`{ name: 'Adib', role: 'reader' }`), username
    login `'nina'` → `'adib'`; pemakaian di `TOKEN_CLAIMS`/`ACCESS_POLICY`
    ikut.
  - `Animation.jsx`: `ninaY`/`setNinaY` → `adibY`/`setAdibY`,
    `ninShape` → `adibShape`, key timeline GSAP `'nina'` → `'adib'`
    (dipakai konsisten di `popIn` dan `T()`).
- **Layout:** koordinat/posisi tetap sama — hanya teks yang tampil berubah
  ("Nina" → "Adib") di: caption akhir Act 2 (`Hash cocok, Adib valid`),
  record buku resepsionis, form login (username `adib`), baris buku bocor
  (`adib: p4ketAman!`), dan stempel `IDENTITAS VALID` (sub `adib terbukti`).
- **Asset:** tidak ada (semua inline SVG; ekspresi/pose karakter tidak
  berubah).
- **Timeline:** tidak berubah (durasi Act, hold budget, dan jumlah key sama;
  hanya nama target GSAP yang ikut rename).
- **Status test setelah perbaikan:** esbuild PASS per-topic; vite dev
  transform HTTP 200. Verifikasi visual menunggu reviewer.

## File yang disentuh

- `src/content/18-auth/data.js`
- `src/content/18-auth/Animation.jsx`
- `src/content/18-auth/_docs/AUTH_PLAN.md` (prosa + daftar data + log §13)
- `src/content/18-auth/revisi/README.md` (index)
- Backup/` di bawah 18-auth TIDAK disentuh (baik sengaja — kontrak 02 §3a).