# Plan: Ganti Icon "Mirip Logo" (AI-generated) → Logo Asli dari Internet

**Scope: `container-docker` SAJA.** Topic lain (linux-vs-unix,
desktop-environment, tailscale) ada di file terpisah: `other-plan.md`
di folder `revisi/` yang sama.

**Masalah**: Icon brand di `container-docker` di-generate lewat ChatGPT
image gen (`vm-icon-generator` extension), sengaja dibuat generik untuk
menghindari trademark. Hasilnya kurang akurat/kurang nempel ke logo asli
yang audiens sudah kenal. Solusi: ambil file logo asli (SVG/PNG resmi)
dari sumber terpercaya di internet, drop-in replace ke
`container-docker/icons/`.

## 1. Audit — Icon di `container-docker/icons/`

| id | File saat ini | Kondisi | Ganti dengan logo asli? |
|---|---|---|---|
| `node-icon` | node-icon.png | ✅ SUDAH diganti (Devicon, 2026-09-07) | Selesai |
| `python-icon` | python-icon.png | ✅ SUDAH diganti (Devicon, 2026-09-07) | Selesai |
| `redis-icon` | redis-icon.png | ✅ SUDAH diganti (Devicon, 2026-09-07) | Selesai |
| `docker-engine-icon` | docker-engine-icon.png | AI-generated, SENGAJA generik krn catatan trademark whale Docker (lihat `revisi/README.md`) | ⚠️ Opsional — whale Docker trademark-nya lebih ketat, tetap generik lebih aman kecuali kamu memang mau pakai logo resmi |

## 2. Sumber Logo Asli

| Kategori | Sumber utama | Format | Lisensi |
|---|---|---|---|
| Bahasa/tools dev (Node, Python, Redis) | Simple Icons (`cdn.jsdelivr.net/npm/simple-icons@latest/icons/{slug}.svg`) | SVG monokrom | CC0 |
| Bahasa/tools dev (versi warna, yang dipakai) | Devicon (`cdn.jsdelivr.net/npm/devicon@latest/icons/{name}/{name}-original.svg`) | SVG warna | MIT |
| Docker whale (kalau Phase B jadi dipakai) | Simple Icons (`docker`) | SVG monokrom | CC0 |

**Catatan**: project ini butuh full-color, jadi sumber final yang dipakai
adalah Devicon (`-original.svg`), bukan Simple Icons yang monokrom.

## 3. Kenapa Download-nya Lewat Terminal Desktop Commander, Bukan Sandbox Claude

Sandbox bash Claude (`bash_tool`) cuma boleh akses domain terbatas (npm,
pypi, github, crates.io, dst) — `cdn.jsdelivr.net` dan
`commons.wikimedia.org` tidak termasuk, jadi curl dari sana akan gagal.

**Solusi**: jalankan curl/wget lewat `Desktop Commander:start_process`,
yaitu terminal asli di komputer kamu (`/home/adb/...`) yang punya akses
internet penuh. Semua download & konversi file terjadi langsung di project
folder kamu, tidak lewat sandbox Claude sama sekali.

## 4. Cara Kerja Download (per icon)

- Cari slug yang benar di Devicon/Simple Icons untuk tiap id
  (mis. `node-icon` → slug `nodedotjs`, `python-icon` → slug `python`,
  `redis-icon` → slug `redis`).
- `curl -sL <url> -o <path-tujuan>.svg` — simpan dengan nama file yang
  SAMA seperti sekarang, supaya `loader.js` & `Animation.jsx` tidak perlu
  diubah sama sekali.
- Convert SVG → PNG pakai `scripts/svg-to-png.mjs` (headless Chrome via
  Puppeteer — ImageMagick bawaan gak support gradient SVG, hasilnya
  hitam polos).
- Simpan versi asli SVG juga di `icons/_originals/` buat arsip/lisensi,
  plus `icons/_originals/LICENSE-LOGOS.md` yang nyantumin sumber tiap
  logo.

**Contoh script** (dijalankan via Desktop Commander terminal):

```bash
cd /home/adb/Projects/Personal/mcp-servers-animation/mcp-servers-animation/src/content/container-docker/icons

curl -sL https://cdn.jsdelivr.net/npm/devicon@latest/icons/nodejs/nodejs-original.svg -o _originals/node-icon.svg
curl -sL https://cdn.jsdelivr.net/npm/devicon@latest/icons/python/python-original.svg -o _originals/python-icon.svg
curl -sL https://cdn.jsdelivr.net/npm/devicon@latest/icons/redis/redis-original.svg   -o _originals/redis-icon.svg

node scripts/svg-to-png.mjs
```

## 5. Urutan Eksekusi (checklist)

### Phase A — Pilot: node, python, redis ✅ SELESAI (2026-09-07)

- [x] Download node-icon, python-icon, redis-icon — sumber final: Devicon
      (bukan Simple Icons, karena Simple Icons monokrom & project ini butuh full-color)
- [x] Convert SVG → PNG pakai `scripts/svg-to-png.mjs`
- [x] File lama (AI-generated) di-backup ke `icons/_originals/backup-ai-generated/`
- [x] Sumber SVG asli + catatan lisensi disimpan di `icons/_originals/*.svg` +
      `icons/_originals/LICENSE-LOGOS.md` (Devicon = MIT)
- [x] `icons.json` batch-2 ditandai status: SUPERSEDED (arsip histori tetap disimpan)
- [x] Commit: `fix(icons): replace container-docker batch-2 (node/python/redis) with real Devicon logos` (64b1387)
- [ ] Cek render di Animation.jsx (appChip Act 1 & 5) — perlu kamu jalanin
      dev server manual buat lihat visualnya, saya belum bisa preview browser

### Phase B — docker-engine-icon ✅ SELESAI (2026-09-07)

- [x] Konfirmasi: pakai logo whale resmi Docker (dikonfirmasi user)
- [x] Download dari Simple Icons (`docker`), fill di-set ke #2496ED, convert via `scripts/svg-to-png.mjs`
- [x] File lama di-backup ke `icons/_originals/backup-ai-generated/docker-engine-icon.png`
- [x] `icons.json` & `LICENSE-LOGOS.md` diupdate
- [x] Commit: `fix(icons): replace container-docker docker-engine-icon with real Docker whale logo` (8d8981b)
- [ ] Cek render di Act 3 (dockerEngineBox) — perlu dev server manual

## 6. Yang TIDAK saya lakukan otomatis

- Saya tidak akan generate/gambar ulang logo pakai AI (itu sumber masalahnya).
- Saya tidak akan pakai logo dari sumber tak jelas lisensinya — hanya
  Simple Icons / Devicon / Wikimedia yang jelas boleh dipakai utk
  identifikasi brand.
- `docker-engine-icon` saya skip dari eksekusi otomatis kecuali kamu
  konfirmasi mau pakai logo resmi (trademark-nya lebih sensitif krn
  produk komersial aktif).

**Next**: konfirmasi Phase B (docker-engine-icon) mau diproses atau tetap
generik.

---

# Plan Tambahan: Icon Baru untuk Momen yang Masih Text-Only

**Konteks**: Selain logo yang kurang akurat, ada masalah kedua —
banyak momen penting di container-docker cuma ditampilkan sebagai
teks polos (Badge/TextCard) tanpa icon pendamping, padahal momen itu
justru yang paling penting buat pemahaman audiens (insight, trade-off,
payoff). Berikut audit lengkap + rencana penambahan icon.

## 1. Cek Elemen "Wajah" di Act 1 (yang ditanyakan)

Posisi persis: di bawah Meter RAM/CPU, di atas hook question
"Kenapa jalanin app kecil aja laptop udah teriak?" — itu adalah
`FaceCharacter`, komponen SVG digambar manual di `Animation.jsx`
(baris ~434), BUKAN emoji Unicode dan BUKAN file icon:

```jsx
const FaceCharacter = ({ x, y, happy }) => (
  <g transform={`translate(${x},${y})`}>
    <circle r={26} fill={COLORS.PANEL} stroke={COLORS.BORDER} strokeWidth={2} />
    <circle cx={-9} cy={-6} r={3} fill={COLORS.TEXT} />   {/* mata kiri */}
    <circle cx={9} cy={-6} r={3} fill={COLORS.TEXT} />    {/* mata kanan */}
    {happy
      ? <path d="M -10 8 Q 0 18 10 8" ... />               {/* senyum */}
      : <path d="M -10 12 Q 0 4 10 12" ... />}              {/* sedih */}
  </g>
)
```

Ini konsisten dengan pola lama di `tailscale` SEBELUM mereka bikin
`face-happy.png` / `face-surprised.png` sebagai file icon asli (lihat
`tailscale/icons/icons.json`). `container-docker` belum di-upgrade ke pola
itu — masih inline SVG.

**Opsi**: (a) biarkan inline SVG (sudah cukup jelas & ringan, bukan
prioritas), atau (b) generate jadi `face-happy-icon.png` /
`face-sad-icon.png` biar konsisten style-nya sama icon lain & gampang
di-tweak desainnya tanpa sentuh kode. Rekomendasi: opsional, prioritas
rendah — bukan sumber masalah utama.

## 2. Audit: Momen Text-Only yang Perlu Icon Baru

| Act | Elemen | Isi teks | Ada icon? |
|---|---|---|---|
| 1 | hookBubble (Badge) | "Kenapa jalanin app kecil aja laptop udah teriak?" | ❌ Tidak |
| 1 | cliffhangerCard (TextCard) | "Masa iya tiap app butuh 1 OS penuh sendiri-sendiri?" | ❌ Tidak |
| 2 | vmInsightBadge (Badge) | "Tiap VM = 1 OS Utuh, Boot dari Nol" | ❌ Tidak |
| 2 | vmCaptionCard (TextCard) | "Bukan app-nya yang berat — OS pembungkusnya." | ❌ Tidak |
| 3 | namespaceLabel (Badge) | "Namespace = sekat privasi" | ❌ Tidak |
| 3 | cgroupLabel (Badge) | "Cgroup = jatah resource" | ❌ Tidak |
| 3 | containerInsightBadge (Badge) | "Container Numpang 1 Kernel yang Sama" | ❌ Tidak |
| 3 | containerPayoffCard (TextCard) | "App + dependency doang, numpang kernel yang sama." | ❌ Tidak |
| 4 | tradeoffQuestionBadge (Badge) | "Kalau container jauh lebih enak, kenapa VM masih dipakai?" | ❌ Tidak |
| 4 | tradeoffCaptionCard (TextCard) | "Container ringan karena numpang kernel..." | ❌ Tidak |
| 5 | closingNoteCard (TextCard) | "VM tetap dipakai kalau butuh isolasi lebih kuat..." | ❌ Tidak |
| 5 | closingLineCard (TextCard) | "Container bukan versi kecil dari VM..." | ❌ Tidak |
| 5 | closingBrandBadge (Badge) | "Itu yang bikin laptop lo lega." | ❌ Tidak |

Total 13 momen teks polos dari 5 Act. `Badge` & `TextCard` (component
generic di file ini) memang belum punya prop `icon` sama sekali — beda
dengan `DiagramBox` yang sudah support icon dari awal.

## 3. Icon Baru yang Diusulkan (batch-4, secukupnya — bukan 50+)

Fokus ke konsep yang paling butuh bantuan visual, bukan asal nambah:

| id icon baru | Dipakai di | Kenapa perlu |
|---|---|---|
| `question-icon` | hookBubble, tradeoffQuestionBadge | Kedua Badge ini isinya PERTANYAAN — icon tanda tanya/mikir bantu audiens langsung sadar "ini pertanyaan, bukan pernyataan" |
| `insight-icon` (lightbulb generik, style konsisten dgn insight-bulb punya desktop-environment) | vmInsightBadge, containerInsightBadge | Kedua Badge ini isinya KESIMPULAN/insight utama tiap Act — icon bulb bikin momen "aha" lebih nempel |
| `namespace-icon` | namespaceLabel | "Sekat privasi" abstrak secara teks — icon sekat/partisi bikin konsep konkret |
| `cgroup-icon` | cgroupLabel | "Jatah resource" abstrak — icon gauge/meter/slice bikin konsep konkret |
| `payoff-check-icon` | closingNoteCard, closingLineCard, closingBrandBadge (Act 5) | Act 5 full teks tanpa aksen visual sama sekali padahal ini bagian PAYOFF (paling penting) — icon checkmark/relief kasih penutup yang memuaskan |

Total 5 icon baru (bukan puluhan), tapi dipakai ulang di 13 momen di
atas → dampaknya besar tapi biaya generate kecil.

## 4. Perubahan Kode yang Menyertai (bukan cuma taruh file PNG)

`Badge` dan `TextCard` perlu prop `icon` opsional (pola sama seperti
`DiagramBox`), supaya icon baru bisa langsung dipasang tanpa ubah
struktur ulang tiap pemanggilan:

```jsx
const Badge = ({ x, y, text, color = COLORS.DOCKER, w = 280, icon = null }) => (
  ...
  {icon && <image href={getIcon(icon)} x={16} y={h/2 - 10} width={20} height={20} />}
  ...
)
```

Lalu tinggal tambah `icon="question-icon"` dkk di masing-masing
pemanggilan yang relevan (13 titik di tabel §2).

## 5. Sumber Icon Baru Ini

Beda dari batch logo brand (§1-2 di atas file ini) — 5 icon ini konsep
generik (tanya, insight, sekat, resource, checklist), BUKAN logo brand,
jadi tetap pakai jalur generator yang sudah ada
(`vm-icon-generator` extension via ChatGPT image-gen), bukan download
dari internet. Cocok ditambahkan sebagai batch-4 baru di
`icons/icons.json`, format sama seperti batch-1/2/3 yang sudah ada.

## 6. Urutan Kerja (checklist)

### Phase 1 — Siapkan icon ✅ SELESAI (2026-09-07)

- [x] Tambah entry batch-4 di `container-docker/icons/icons.json` (5 icon
      di atas + prompt generate-nya) — commit 858abf4
- [x] Generate lewat extension `vm-icon-generator` — user sudah generate
- [x] Verifikasi 5 file PNG masuk ke `icons/`: question-icon.png,
      insight-icon.png, namespace-icon.png, cgroup-icon.png,
      payoff-check-icon.png — semua RGBA (transparan), warna sesuai spec
- [x] Update `loader.js` supaya 5 icon baru ke-load

### Phase 2 — Ubah component ✅ SELESAI (2026-09-07, commit 858abf4)

- [x] Tambah prop `icon` opsional ke `Badge`
- [x] Tambah prop `icon` opsional ke `TextCard`

### Phase 3 — Pasang icon di 8 titik wajib (per Act) ✅ SELESAI (2026-09-07, commit 858abf4)

- [x] Act 1: hookBubble → question-icon
- [ ] Act 1: cliffhangerCard → (opsional, belum diputuskan — biarkan dulu tanpa icon)
- [x] Act 2: vmInsightBadge → insight-icon
- [ ] Act 2: vmCaptionCard → (opsional, belum diputuskan)
- [x] Act 3: namespaceLabel → namespace-icon
- [x] Act 3: cgroupLabel → cgroup-icon
- [x] Act 3: containerInsightBadge → insight-icon
- [ ] Act 3: containerPayoffCard → (opsional, belum diputuskan)
- [x] Act 4: tradeoffQuestionBadge → question-icon
- [ ] Act 4: tradeoffCaptionCard → (opsional, belum diputuskan)
- [x] Act 5: closingNoteCard → payoff-check-icon
- [x] Act 5: closingLineCard → payoff-check-icon
- [x] Act 5: closingBrandBadge → payoff-check-icon

### Phase 4 — Review

- [ ] Preview tiap Act, cek layout tidak numpuk/kegeser — perlu dev server manual
- [x] Cek ukuran icon konsisten (20px, pola sama seperti DiagramBox 28px tapi diperkecil krn Badge/TextCard lebih ramping)
- [x] Commit: `feat(container-docker): add icon support to Badge/TextCard + batch-4 icon plumbing` (858abf4)
- [x] Build syntax check via esbuild — lolos, tidak ada error
- [x] batch-4 PNG masuk, loader.js diupdate, build check ke-2 lolos — commit menyusul

**Semua 5 icon batch-4 sudah live** — tidak perlu ubah Animation.jsx lagi,
tinggal `loader.js` yang diupdate. Preview visual (dev server) masih perlu
kamu cek manual buat pastikan layout Badge/TextCard nggak numpuk sama icon
baru ini.

### Opsional — Face icon (§1, prioritas rendah)

- [ ] Konfirmasi: mau convert FaceCharacter inline SVG jadi file icon
      (face-happy-icon.png / face-sad-icon.png) atau biarkan apa adanya?

**Next**: konfirmasi apakah mau saya lanjut generate batch-4 ini
sekarang, atau kamu mau revisi dulu daftar 5 icon di atas (nama/konsep
icon-nya).
